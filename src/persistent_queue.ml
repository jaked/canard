type item = {
  add_time : int64;
  expiry : int64;
  data : string;
  xid : int32;
}

type config = {
}

type t = {
}

let make persistence_path name config = failwith "unimplemented"

let initialized = Lwt_cond.

let add value expiry =
  
